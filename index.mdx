---
title: "Nabla Developer Docs"
description: "The clinical AI platform for builders."
mode: "custom"
---

export const eyebrowStyle = { fontSize: 12, letterSpacing: "0.14em" };
export const cardStyle = {
  padding: "22px 22px 24px",
  borderWidth: 1,
  borderStyle: "solid",
  borderColor: "var(--landing-border)",
  borderRadius: 12,
  background: "var(--landing-surface)",
  display: "block",
  color: "inherit",
  textDecoration: "none",
  transition: "transform 0.15s ease, border-color 0.15s ease, background 0.15s ease",
};
export const iconBoxStyle = {
  width: 36,
  height: 36,
  borderRadius: 8,
  borderWidth: 1,
  borderStyle: "solid",
  borderColor: "var(--landing-border)",
  background: "var(--landing-icon-bg)",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  color: "var(--landing-fg-strong)",
};
export const btnPrimary = {
  display: "inline-flex",
  alignItems: "center",
  gap: 10,
  height: 48,
  padding: "0 22px",
  borderRadius: 10,
  background: "var(--landing-btn-primary-bg)",
  color: "var(--landing-btn-primary-fg)",
  border: "1px solid var(--landing-btn-primary-bg)",
  fontSize: 15,
  fontWeight: 500,
  textDecoration: "none",
};
export const btnSecondary = {
  display: "inline-flex",
  alignItems: "center",
  gap: 10,
  height: 48,
  padding: "0 22px",
  borderRadius: 10,
  background: "transparent",
  color: "var(--landing-fg)",
  border: "1px solid var(--landing-border-strong)",
  fontSize: 15,
  fontWeight: 500,
  textDecoration: "none",
};
export const sectionEyebrow = {
  fontSize: 12,
  letterSpacing: "0.14em",
  color: "var(--landing-fg-subtle)",
  marginBottom: 18,
};
export const sectionTitle = {
  fontWeight: 700,
  lineHeight: 1.02,
  letterSpacing: "-0.025em",
  margin: 0,
  marginBottom: 24,
};
export const sectionSub = {
  fontSize: 17,
  lineHeight: 1.55,
  color: "var(--landing-fg-muted)",
  maxWidth: "44ch",
  margin: 0,
};

<div className="mx-auto" style={{ maxWidth: 1180, padding: "80px 32px 96px", color: "var(--landing-fg)" }}>

  {/* ============== HERO ============== */}
  <section>
    <div className="font-mono uppercase flex items-center" style={{ ...eyebrowStyle, color: "var(--landing-fg-subtle)", marginBottom: 28, gap: 12 }}>
      <span style={{ display: "inline-block", width: 28, height: 1, background: "var(--landing-rule)" }} />
      NABLA CORE API · V2025-09-01
    </div>

    <h1 className="font-bold" style={{ fontSize: "clamp(40px, 6.4vw, 84px)", lineHeight: 0.98, letterSpacing: "-0.035em", maxWidth: "16ch", margin: 0, marginBottom: 28 }}>
      The clinical AI platform for builders.
    </h1>

    <p style={{ fontSize: "clamp(17px, 1.4vw, 20px)", lineHeight: 1.55, color: "var(--landing-fg-muted)", maxWidth: "56ch", margin: 0, marginBottom: 36 }}>
      Real-time medical transcription, structured note generation, and normalized
      clinical data — accessible through a single, consistent API.
    </p>

    <div className="flex flex-wrap" style={{ gap: 12, marginBottom: 72 }}>
      <a href="/core-api/quickstart" style={btnPrimary}>
        Quickstart <span aria-hidden="true">→</span>
      </a>
      <a href="/core-api/api-reference/server/transcribe-ws" style={btnSecondary}>
        Read the API reference
      </a>
    </div>

    {/* Demo card */}
    <div className="grid grid-cols-1 md:grid-cols-2 overflow-hidden" style={{ border: "1px solid var(--landing-border)", borderRadius: 16, background: "var(--landing-surface)" }}>

      {/* Live transcript */}
      <div style={{ padding: 32, borderRight: "1px solid var(--landing-border)" }}>
        <div className="font-mono uppercase" style={{ ...eyebrowStyle, color: "var(--landing-fg-subtle)", marginBottom: 22 }}>
          LIVE TRANSCRIPT · <span style={{ color: "var(--landing-fg-faint)" }}>00:42</span>
        </div>
        <div className="flex flex-col" style={{ gap: 14 }}>
          {[
            { ts: "00:08", speaker: "Clinician", color: "var(--landing-speaker-clinician)", text: "Hi, what brings you in today?" },
            { ts: "00:11", speaker: "Patient", color: "var(--landing-speaker-patient)", text: "I've had this dry cough for about three days, and last night I was really short of breath." },
            { ts: "00:24", speaker: "Clinician", color: "var(--landing-speaker-clinician)", text: "Any wheezing, fever?" },
          ].map((l) => (
            <div key={l.ts} style={{ display: "grid", gridTemplateColumns: "48px 1fr", gap: 14, fontSize: 14.5, lineHeight: 1.55 }}>
              <span className="font-mono" style={{ fontSize: 12, color: "var(--landing-fg-faint)", paddingTop: 3 }}>{l.ts}</span>
              <p style={{ margin: 0, color: "var(--landing-fg-strong)" }}>
                <span style={{ fontWeight: 600, color: l.color }}>{l.speaker}:</span> {l.text}
              </p>
            </div>
          ))}
          <div style={{ display: "grid", gridTemplateColumns: "48px 1fr", gap: 14, fontSize: 14.5, lineHeight: 1.55 }}>
            <span className="font-mono" style={{ fontSize: 12, color: "var(--landing-fg-faint)", paddingTop: 3 }}>00:31</span>
            <p style={{ margin: 0, color: "var(--landing-fg-strong)" }}>
              <span style={{ fontWeight: 600, color: "var(--landing-speaker-patient)" }}>Patient:</span> Some wheezing,{" "}
              <span style={{ display: "inline-block", background: "var(--landing-pill-bg)", color: "var(--landing-pill-fg)", padding: "1px 8px", borderRadius: 5, fontSize: 13.5 }}>no fever</span>
              <span className="animate-pulse" style={{ display: "inline-block", width: 7, height: 14, background: "var(--landing-cursor)", verticalAlign: -2, marginLeft: 4 }} />
            </p>
          </div>
        </div>
      </div>

      {/* Generated note */}
      <div style={{ padding: 32 }}>
        <div className="font-mono uppercase" style={{ ...eyebrowStyle, color: "var(--landing-fg-subtle)", marginBottom: 22 }}>
          <span style={{ color: "var(--landing-fg-faint)", marginRight: 4 }}>✦</span> GENERATED NOTE · GENERIC_SOAP
        </div>
        <h4 className="font-mono uppercase" style={{ fontSize: 11.5, letterSpacing: "0.14em", fontWeight: 600, margin: 0, marginBottom: 8 }}>SUBJECTIVE</h4>
        <p style={{ margin: 0, marginBottom: 18, fontSize: 14.5, lineHeight: 1.6, color: "var(--landing-fg-strong)" }}>
          Patient reports a 3-day history of dry cough with new-onset dyspnea overnight. Endorses wheezing. Denies fever.
        </p>
        <h4 className="font-mono uppercase" style={{ fontSize: 11.5, letterSpacing: "0.14em", fontWeight: 600, margin: 0, marginBottom: 8 }}>ASSESSMENT</h4>
        <p style={{ margin: 0, fontSize: 14.5, lineHeight: 1.6, color: "var(--landing-fg-subtle)", fontStyle: "italic" }}>
          Streaming…
          <span className="animate-pulse" style={{ display: "inline-block", width: 7, height: 14, background: "var(--landing-cursor)", verticalAlign: -2, marginLeft: 4, fontStyle: "normal" }} />
        </p>
      </div>
    </div>
  </section>

  <hr style={{ border: 0, borderTop: "1px solid var(--landing-border)", margin: "96px 0" }} />

  {/* ============== CAPABILITIES ============== */}
  <section>
    <div className="grid grid-cols-1 items-start" style={{ gap: 56, gridTemplateColumns: "minmax(0, 5fr) minmax(0, 7fr)" }}>
      <div>
        <div className="font-mono uppercase" style={sectionEyebrow}>§01 — CAPABILITIES</div>
        <h2 className="font-bold" style={{ ...sectionTitle, fontSize: "clamp(30px, 3.2vw, 44px)" }}>
          Built for clinical<br/>workflows, not chat
        </h2>
        <p style={sectionSub}>
          Composable primitives that match how clinicians actually work — capture,
          summarize, and structure encounters in a single pass.
        </p>
      </div>

      <div className="grid grid-cols-1 sm:grid-cols-3" style={{ gap: 14 }}>
        {[
          { href: "/core-api/guides/stream-a-live-encounter", icon: "microphone", title: "Transcribe", body: "Stream audio or upload a recording. Diarized, medical-grade transcripts in 35+ languages." },
          { href: "/core-api/guides/generate-a-clinical-note", icon: "file-lines", title: "Note generation", body: "SOAP, APSO, specialty templates, or your own — generated synchronously or async." },
          { href: "/core-api/guides/extract-fhir-normalized-data", icon: "layer-group", title: "Normalized data", body: "FHIR-aligned problems, medications, allergies, and orders, ready for your EHR." },
        ].map((c) => (
          <a key={c.title} href={c.href} style={cardStyle}>
            <div style={{ ...iconBoxStyle, marginBottom: 36 }}>
              <Icon icon={c.icon} size={18} />
            </div>
            <h3 style={{ fontSize: 17, fontWeight: 600, letterSpacing: "-0.01em", margin: 0, marginBottom: 8 }}>{c.title}</h3>
            <p style={{ fontSize: 14, lineHeight: 1.55, color: "var(--landing-fg-muted)", margin: 0 }}>{c.body}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <hr style={{ border: 0, borderTop: "1px solid var(--landing-border)", margin: "96px 0" }} />

  {/* ============== QUICKSTART STEPS ============== */}
  <section>
    <div className="grid grid-cols-1 items-start" style={{ gap: 56, gridTemplateColumns: "minmax(0, 5fr) minmax(0, 7fr)" }}>
      <div>
        <div className="font-mono uppercase" style={sectionEyebrow}>§02 — START BUILDING</div>
        <h2 className="font-bold" style={{ ...sectionTitle, fontSize: "clamp(30px, 3.2vw, 44px)" }}>
          From zero to first note<br/>in under five minutes.
        </h2>
        <p style={sectionSub}>
          A guided path from your first API call to a webhook-driven EHR integration.
        </p>
      </div>

      <div className="grid grid-cols-1 sm:grid-cols-2" style={{ gap: 14 }}>
        {[
          { num: "01", href: "/core-api/quickstart", icon: "bolt", title: "Quickstart", body: "Authenticate, transcribe a clip, and generate a clinical note end-to-end." },
          { num: "02", href: "/core-api/authentication", icon: "key", title: "Authenticate", body: "Set up OAuth 2.0 for the Server API or mint short-lived User API tokens." },
          { num: "03", href: "/core-api/webhooks/setup", icon: "bell", title: "Webhooks", body: "Receive HMAC-signed events for transcripts, notes, and async jobs." },
          { num: "04", href: "/core-api/guides/ehr-integration", icon: "hospital", title: "EHR integration", body: "Push notes into your EHR from the browser or your backend." },
        ].map((s) => (
          <a key={s.num} href={s.href} style={cardStyle}>
            <div className="flex items-center justify-between" style={{ marginBottom: 36 }}>
              <div style={iconBoxStyle}>
                <Icon icon={s.icon} size={18} />
              </div>
              <span className="font-mono" style={{ fontSize: 12, color: "var(--landing-fg-faint)", letterSpacing: "0.1em" }}>{s.num}</span>
            </div>
            <h3 style={{ fontSize: 17, fontWeight: 600, letterSpacing: "-0.01em", margin: 0, marginBottom: 8 }}>{s.title}</h3>
            <p style={{ fontSize: 14, lineHeight: 1.55, color: "var(--landing-fg-muted)", margin: 0 }}>{s.body}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <hr style={{ border: 0, borderTop: "1px solid var(--landing-border)", margin: "96px 0" }} />

  {/* ============== FOOTER CTA ============== */}
  <section className="flex flex-wrap items-end justify-between" style={{ gap: 32 }}>
    <div>
      <div className="font-mono uppercase" style={sectionEyebrow}>§03 — SUPPORT</div>
      <h2 className="font-bold" style={{ ...sectionTitle, fontSize: "clamp(30px, 3.2vw, 44px)", marginBottom: 12 }}>Talk to a human.</h2>
      <p style={sectionSub}>
        Integration help, account questions, or product feedback — we read every message.
      </p>
    </div>
    <a href="mailto:hi@nabla.com" style={btnPrimary}>
      Contact support <span aria-hidden="true">→</span>
    </a>
  </section>

</div>
